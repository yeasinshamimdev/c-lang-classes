#include <stdio.h>
#include <stdbool.h>

int main(){
  float a = 15;
  int b = 2;
  // int sum = a + b;
  float div = a / b;
  bool sign = true;
  printf("%d", sign);
  // printf("The division of %.0f and %d is: %.1f\n", a, b, div);

  // printf("The sum of %d and %d is: %d\n", a, b, sum);

  return 0;
}